#include <iostream>
#include <map> 
#include <vector> 
#include "adjacent.cc"
using namespace std; 

map<int, vector<int>> adjacentEdges() {
    map<int, vector<int>> return; 
    vector<int> edges;
    for (int i = 0; i < 54; ++i) {

    }
 
}